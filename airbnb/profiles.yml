airbnb:
  outputs:
    dev:
      type: snowflake
      account: MMOJZBP-TX79176
      user: dbt

      role: TRANSFORM
      private_key: "{{ env_var('SNOWFLAKE_PRIVATE_KEY_PATH') }}"
      private_key_passphrase: "{{ env_var('SNOWFLAKE_PRIVATE_KEY_PASSPHRASE') }}"
      database: AIRBNB
      schema: DEV
      threads: 1
      warehouse: COMPUTE_WH
  target: dev
